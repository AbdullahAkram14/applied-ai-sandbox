# M7 — Multi-Tool Workflows

## Feature I picked

* Add note search so users can filter notes by title or body text.

## Plan from chat

Feature: Note Search

Data model:

* No note dictionary changes are needed.
* Existing notes already have `title` and `body`, which are enough for searching.

Route changes:

* Update the home route (`/`) to read an optional query parameter called `q`.
* If `q` is empty or missing, show all notes.
* If `q` has text, filter notes where the title or body contains the search term.
* Search should be case-insensitive.

UI changes:

* Add a small search form near the top of the notes list.
* The form should use `method="GET"` and submit to `/`.
* The input should be named `q`.
* Preserve the current search text in the input field after submission.

Edge cases:

* Empty search should show all notes.
* Search should work for title matches.
* Search should work for body matches.
* Search should not crash when there are no notes.
* Search should show zero matching notes if nothing matches.

Acceptance criteria:

* Searching by title filters matching notes.
* Searching by body filters matching notes.
* Empty search shows all notes.
* Existing note creation still works.

## Execute in Claude Code

* Branch: m7-feature-search
* One thing Claude flagged that the chat plan missed: Claude added dedicated tests for title matches, body matches, no matches, empty searches, and case-insensitive searching.

## Polish with Copilot
- Two small edits I accepted:
  - Added small formatting/documentation improvements suggested during editing.
  - Cleaned up naming or comments to make the search feature easier to understand.

## Review in chat
- One useful piece of feedback: The review pointed out that search should handle case-insensitive matches and empty searches, which helped confirm the tests covered realistic user behavior.

## Parallel sessions
- Second task: Add a comment above the home route explaining that it handles note listing and search.
- Worktree path: ../sandbox-task-b
- Honest answer — did I actually parallelize or did one session sit idle?
  - I briefly ran two Claude Code sessions in separate worktrees, but one mostly sat idle because the task was very small. I can see how this would be more useful on larger features.

## Workflow retro

* Not completed yet.
